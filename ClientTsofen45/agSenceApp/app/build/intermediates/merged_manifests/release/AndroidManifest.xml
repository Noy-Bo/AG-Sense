<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tsofen.agsenceapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="24"
        android:targetSdkVersion="30" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Include required permissions for Google Maps API to run. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:extractNativeLibs="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:testOnly="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true" >
        <activity android:name="com.tsofen.agsenceapp.activities.AccountStatusFilterUser" />
        <activity android:name="com.tsofen.agsenceapp.activities.BackBaseActivity" />
        <activity android:name="com.tsofen.agsenceapp.activities.SearchBaseActivity" />
        <activity
            android:name="com.tsofen.agsenceapp.activities.AdminNotification"
            android:label="@string/AdminNotification" />
        <activity
            android:name="com.tsofen.agsenceapp.activities.DeviceSetting"
            android:label="Device Settings" />
        <activity
            android:name="com.tsofen.agsenceapp.activities.AccountStatusFilter"
            android:label="Accounts Status" />
        <activity
            android:name="com.tsofen.agsenceapp.activities.EditAccount"
            android:label="@string/EditAccount" />
        <activity
            android:name="com.tsofen.agsenceapp.activities.EditDevice"
            android:label="@string/EditDevice" />
        <activity
            android:name="com.tsofen.agsenceapp.activities.HealthCareDashboard"
            android:label="@string/healthCareReportsString" />
        <activity
            android:name="com.tsofen.agsenceapp.activities.NewAccount"
            android:label="@string/NewAccount" />
        <activity
            android:name="com.tsofen.agsenceapp.activities.NewDevice"
            android:label="@string/NewDevice" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name="com.tsofen.agsenceapp.activities.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name="com.tsofen.agsenceapp.activities.DeviceStatusList"
            android:label="Device Status List" />
        <activity
            android:name="com.tsofen.agsenceapp.activities.DeviceView"
            android:label="Device View" />
        <activity
            android:name="com.tsofen.agsenceapp.activities.DeviceFilter"
            android:label="@string/Filter" />
        <activity
            android:name="com.tsofen.agsenceapp.activities.AdminDashboardActivity"
            android:label="Admin Dashboard" />
        <activity
            android:name="com.tsofen.agsenceapp.activities.AccountDashboardActivity"
            android:label="Account Dashboard"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="com.tsofen.agsenceapp.activities.DeviceStatus"
            android:label="Device Status" />
        <activity android:name="com.tsofen.agsenceapp.activities.LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <!-- Needs to be explicitly declared on P+ -->
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <provider
            android:name="androidx.lifecycle.ProcessLifecycleOwnerInitializer"
            android:authorities="com.tsofen.agsenceapp.lifecycle-process"
            android:exported="false"
            android:multiprocess="true" />

        <activity
            android:name="com.google.android.gms.common.api.GoogleApiActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>