package com.tsofen.agsenceapp.entities;

public class Admin extends User {
    public Admin(int id, String userName, String email, String userType) {
        super(id, userName, email, userType);
    }
}
